.. _installation:

Installation
============
This is a guide to installing the `pyvisual` package.

.. _installation_pip:
Installing via pip
----------------------
You can install the package using pip. Run the following command in your terminal:
.. code-block:: bash

    pip install pyvisual